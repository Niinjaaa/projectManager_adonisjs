<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Users</title>
</head>
<body>
    <h1>Update Users</h1>

    <form action="{{ route('users.update', {id: users.id}) }}?_method=PUT" method="post">
        <label for="name">Nom</label>
        <input type="text" value="{{ users.name }}" name="name">

        <label for="email">Description</label>
        <input type="email" value="{{ users.email }}" name="email">

        <label for="password">DeadLine</label>
        <input type="password" value="{{ users.password }}" name="password">

        <button type="submit">Enregistrer</button>
    </form>
</body>
</html>