<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Users</title>
</head>
<body>
    <h1>Utilisateurs</h1>

    <a href="{{ route('users.create') }}">Créer un utilisateur</a>

    @if(flashMessages.has('success'))
    <div class="success">
        {{ flashMessages.get('success') }}
    </div>
    @endif

    <table class="mt-6">
        <thead>
            <tr>
                <td>ID</td>
                <td>Nom</td>
                <td>Email</td>
                <td>Password</td>
                <td>Action</td>
            </tr>
        </thead>
        <tbody>
            @each(users in user)
                <tr>
                    <td>{{ users.id }}</td>
                    <td>{{ users.name }}</td>
                    <td>{{ users.email }}</td>
                    <td>{{ users.password }}</td>
                    <td> 
                        <form action="{{ route('users.delete', {id: users.id}) }}?_method=DELETE" method="post">
                            <button type="submit" class="button is-danger">
                                Delete
                            </button>
                        </form>
                        <form action="{{ route('users.edit', {id: users.id}) }}" method="get">
                            <button type="submit">
                                Edit
                            </button>
                        </form>
                    </td>
                </tr>
            @endeach
        </tbody>
    </table>
</body>
</html>